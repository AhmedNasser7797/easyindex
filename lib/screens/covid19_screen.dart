import 'package:flutter/material.dart';

class Covid19Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Covid 19 Screen'),
      ),
    );
  }
}
